## minimemo 🐑

Tiny desktop memos without advertisement 🐑

Install & run (from project folder):

```bash
npm install
npm start
```

- `src/main` – Electron main process 🐑  
- `src/renderer` – memo windows & UI 🐑  
- `data` – saved memos (JSON) 🐑

